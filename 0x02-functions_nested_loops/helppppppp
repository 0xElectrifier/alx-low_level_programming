**
 *      if (lastDigit > 9)
 *      {
 *              lastDigit = lastDigit % 10;
 *
 *              while (lastDigit > 9)
 *                      lastDigit = lastDigit % 10;
 *      }
 */
if ((i == '2') && (j == '3') &&
                                                (k == '5') && (l == '9'))
                                                break;

