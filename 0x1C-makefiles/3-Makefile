CC = gcc
SRC = main.c school.c
OBJ = $(SRC:%.c=%.o)
NAME = school
RM = rm -f *.swp *~ *.o ${NAME}

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	rm -f *.swp *~ ${NAME}

oclean:
	rm -f ${OBJ}

fclean:
	${RM}

re: ${OBJ}
	rm -f ${OBJ}
	$(CC) ${OBJ} -o ${NAME}
